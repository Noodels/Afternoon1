void compute_programcounter(Afternoon1*, Afternoon1*);
void compute_cache(Afternoon1*, Afternoon1*);
void compute_memaccess(Afternoon1*, Afternoon1*);

// The value of whatever the pc/cache is driving onto the data bus
uint8_t pc_result(Afternoon1*);
uint8_t cache_result(Afternoon1*);
