/*
 * Compute BLU, just carry registers are changed directly
 */
void compute_blu(Afternoon1*, Afternoon1*);

/*
 * Get result of whatever function should be going on
 */
uint8_t blu_result(Afternoon1*);
