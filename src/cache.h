void compute_codecache(Afternoon1*, Afternoon1*);

// The value of whatever the code cache is driving onto the databus
// Typically, this'll be from LDI instructions
uint8_t codecache_result(Afternoon1*);
